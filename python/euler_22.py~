def getNames():
    File = open("names.txt", "r")
    names = []
    tempName = ""
    c = f.read(1)
    while c:
        if c == '"':
            if tempName != "":
                names.append(tempName)
                tempName = ""
        else:
            tempName += c
        c = f.read(1)
    return names
                
            
def main():
    """Does everything"""
    print getNames()
    
if __name__ == "__main":
    main()
